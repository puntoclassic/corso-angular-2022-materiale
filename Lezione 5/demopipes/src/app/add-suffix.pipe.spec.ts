import { AddSuffixPipe } from './add-suffix.pipe';

describe('AddSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new AddSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
